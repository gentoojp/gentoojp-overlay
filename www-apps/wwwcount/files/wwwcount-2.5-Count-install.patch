--- Count-install.orig	2005-05-15 13:46:19.000000000 +0900
+++ Count-install	2005-05-15 14:26:00.192305376 +0900
@@ -31,9 +31,9 @@
 mirror_file="mirror_count.sh"
 mirror_script="./utils/mirror/$mirror_file"
 
-UserId="nobody"
+UserId="apache"
 userid_flag=0
-GroupId="nobody"
+GroupId="apache"
 groupid_flag=0
 Digits="zero.gif one.gif two.gif three.gif four.gif five.gif\
  six.gif seven.gif eight.gif nine.gif"
@@ -115,19 +115,19 @@
 
 . $Tmpl
 
-if [ ! -f ./$CONFIG_FILE ]
-then
-
-cat <<EOC
-
-    The configuration file $CONFIG_FILR is not created!
-    please create it first by running the script ./Gen-conf
-
-    Aborting installation!
-EOC
-
-    exit 1
-fi
+#if [ ! -f ./$CONFIG_FILE ]
+#then
+#
+#cat <<EOC
+#
+#    The configuration file $CONFIG_FILR is not created!
+#    please create it first by running the script ./Gen-conf
+#
+#    Aborting installation!
+#EOC
+#
+#    exit 1
+#fi
 
 tdir="./tmp"
 
@@ -208,7 +208,8 @@
 
 echo "Press Control-C to abort"
 echo $n "Continue [y|n]$c? "
-read answer
+#read answer
+answer=y
 
 if [ .$answer = . ]
 then
@@ -218,7 +219,8 @@
     do
         echo "Press Control-C to abort"
         echo $n "Continue [y|n]$c? " 
-        read answer
+#        read answer
+	answer=y
             if [ .$answer = . ]
             then
                 answer=x
@@ -250,7 +252,8 @@
 
 EOU
 echo $n "*Do you know the user and group id of httpd' child process [y|n]:$c? "
-   read answer
+#   read answer
+   answer=y
    if [ .$answer = . ]
    then
         answer=x
@@ -258,7 +261,8 @@
    until [ $answer = "y" -o $answer = "Y" -o $answer = "n" -o $answer = "N" ]
    do
 echo $n "*Do you know the user and group id of httpd' child process [y|n]:$c? "
-   read answer
+#   read answer
+   answer=y
    if [ .$answer = . ]
    then
         answer=x
@@ -273,7 +277,8 @@
 # enter user id -starts
 ###
    echo $n "*Enter user id of httpd's child process [no default]:$c? "
-   read answer
+#   read answer
+   answer=apache
    if [ .$answer = . ]
    then 
         answer=x
@@ -281,7 +286,8 @@
    until [ $answer != "x" ]
    do
    echo $n "*Enter user id of httpd's child process [no default]:$c? "
-   read answer
+#   read answer
+   answer=apache
    if [ .$answer = . ]
    then
         answer=x
@@ -297,7 +303,8 @@
 # enter groud id -starts
 ##
    echo $n "*Enter group id of httpd's child process [no default]:$c? "
-   read answer
+#   read answer
+   answer=apache
    if [ .$answer = . ]
    then 
         answer=x
@@ -305,7 +312,8 @@
    until [ $answer != "x" ]
    do
    echo $n "*Enter group id of httpd's child process [no default]:$c? "
-   read answer
+#   read answer
+   answer=apache
    if [ .$answer = . ]
    then
         answer=x
@@ -337,6 +345,13 @@
 	echo ""
 fi
 
+echo "installing VERSION->$CONFIG_DIR"
+$INSTALL_PROG -c VERSION $CONFIG_DIR/VERSION
+echo "installing Config.tmpl->$CONFIG_DIR"
+$INSTALL_PROG -c Config.tmpl $CONFIG_DIR/Config.tmpl
+echo "installing Gen-conf->$CONFIG_DIR"
+$INSTALL_PROG -c -m 755 Gen-conf $CONFIG_DIR/Gen-conf
+
 echo "installing sample datafile $SAMPLE_DAT->$DATA_DIR"
 
 if [ ! -f $DATA_DIR/sample.dat ]; then
@@ -425,8 +440,8 @@
 EOM
 echo "installing Count.cgi->$CGIBIN_DIR"
 $INSTALL_PROG -c -m 755 $Binary $CGIBIN_DIR/Count.cgi
-echo "installing $CONFIG_FILE->$CONFIG_DIR"
-$INSTALL_PROG -c $CONFIG_FILE $CONFIG_DIR/$CONFIG_FILE
+echo "installing Gen-conf->$CONFIG_DIR"
+$INSTALL_PROG -c -m 755 Gen-conf $CONFIG_DIR/Gen-conf
 echo "installing sample datafile $SAMPLE_DAT->$DATA_DIR"
 $INSTALL_PROG -c $SAMPLE_DAT $DATA_DIR/sample.dat
 
