diff -Naur gcvs-1.0.orig/common/UCvsFiles.cpp gcvs-1.0/common/UCvsFiles.cpp
--- gcvs-1.0.orig/common/UCvsFiles.cpp	2002-08-13 20:46:40.000000000 +0900
+++ gcvs-1.0/common/UCvsFiles.cpp	2004-09-16 12:44:38.330076000 +0900
@@ -425,7 +425,7 @@
 void UCvsFiles::ResetView(const char *path, bool notifyBrowser)
 {
 	m_path = path;
-	ResetView(true);
+	ResetView(true, false);
 
 	UStr title(UCvsApp::gApp->GetAppName());
 	title << ": ";
@@ -1214,7 +1214,7 @@
 {
 	UStr uppath, filename;
 	SplitPath(m_path, uppath, filename);
-	ResetView(uppath, true);
+	ResetView(uppath.c_str(), true);
 	return 0;
 }
 
@@ -1895,7 +1895,7 @@
 	fullpath << (*data)[EntnodeData::kName];
 	if(data->GetType() == ENT_SUBDIR)
 	{
-		ResetView(fullpath, true);
+		ResetView(fullpath.c_str(), true);
 	}
 	else
 	{
diff -Naur gcvs-1.0.orig/common/UCvsFolders.cpp gcvs-1.0/common/UCvsFolders.cpp
--- gcvs-1.0.orig/common/UCvsFolders.cpp	2002-08-03 00:05:02.000000000 +0900
+++ gcvs-1.0/common/UCvsFolders.cpp	2004-09-16 12:37:35.038084000 +0900
@@ -519,7 +519,7 @@
 	{
 		UEventSendMessage(GetWidID(), EV_TREE_EXPAND, UMAKEINT(kUMainWidget, 0), traverse.m_item);
 		if(notifyView && UCvsApp::gApp->GetFilesView())
-			UCvsApp::gApp->GetFilesView()->ResetView(path);
+			UCvsApp::gApp->GetFilesView()->ResetView(path, false);
 	}
 }
 
@@ -563,7 +563,7 @@
 	// because that's already done
 	if(notifyView)
 	{
-		UCvsApp::gApp->GetFilesView()->ResetView(forceReload);
+		UCvsApp::gApp->GetFilesView()->ResetView(forceReload, false);
 	}
 	UEventSendMessage(GetWidID(), EV_CHGFOCUS, kUMainWidget, 0L);
 }
@@ -828,7 +828,7 @@
 		if(fileView != 0L)
 		{
 			ASSERT(fileView->IsKindOf(URUNTIME_CLASS(UCvsFiles)));
-			fileView->ResetView(path);
+			fileView->ResetView(path.c_str(), false);
 		}
 	}
 
